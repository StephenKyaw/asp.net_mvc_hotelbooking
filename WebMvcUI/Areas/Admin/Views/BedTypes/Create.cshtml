@model BedTypeViewModel
@{
    ViewData["Title"] = ViewBag.Title;
}
<div class="card">
    <div class="card-body">
        <div class="row">
            <div class="col-4">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group mb-3">
                        <label asp-for="BedTypeName" class="control-label"></label>
                        <input asp-for="BedTypeName" class="form-control" />
                        <span asp-validation-for="BedTypeName" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3 d-flex">
                        <input type="submit" value="Create" class="btn btn-success me-2" />
                        <a class="btn btn-secondary" asp-action="Index">Back to List</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
