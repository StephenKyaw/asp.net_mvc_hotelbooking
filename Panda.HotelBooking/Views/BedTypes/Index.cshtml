@model IEnumerable<Panda.HotelBooking.Models.BedType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BedTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BedTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BedTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BedTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BedTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
